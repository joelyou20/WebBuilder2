@if (Errors.Any())
{
    @foreach (ApiError error in Errors)
    {
        <MudAlert Severity="@Severity.Error" ShowCloseIcon="true">@($"Code: {error.Code}; Message: {error.Message}; Resource: {error.Resource}; Field: {error.Field};")</MudAlert>
    }
}